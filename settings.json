{
  "archive": {
    "dir": "archive",
    "dir_comment": "The directory where data imports will be stored.",
    "schema": {
      "0": "release_version",
      "1": "build_version"
    },
    "schema_comment": [
      "The internal structure of the archive directory.",
      "Each schema item corresponds to a unique metadata tag that must be present for an import to be successful."
    ]
  },
  "data_cache": "./master_data.csv",
  "meta_ext": ".meta",
  "meta_ext_comment": "The file extension of metadata files.",
  "verification": {
    "additional_required_tags": [
      "date"
    ],
    "additional_required_tags_comment": "All other required metadata tags go here."
  },
  "version": "1.2.0"
}